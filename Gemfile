source 'https://rubygems.org'

group :deployment do
  gem 'capistrano', '<3'
  gem 'railsless-deploy'
  gem 'newrelic_api'
  gem 'activeresource' # Required for newrelic_api; missing dependency declaration in gem.
  gem 'graphite-notify', :git => 'git@github.com:gwilym/graphite-notify.git'
end
